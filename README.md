# RISC-V-assembler
A custom RISC-V RV32I Assembler built in Python that converts assembly code into 32-bit binary machine instructions. The project supports multiple instruction formats, label handling, immediate validation, error detection, and register encoding while implementing core concepts of ISA design, and assembly language processing.
